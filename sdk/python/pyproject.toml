[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kiln-sdk"
version = "0.1.0"
description = "Thin typed REST client for Kiln Core"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
