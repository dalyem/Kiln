# Workflow runtime boundary

Planned after the compute, browser and artifact planes. See [workflow architecture](../../docs/architecture/workloads-and-workflows.md). The Python client scaffold is under sdk/python; it does not execute workflows yet.
