# Kiln JavaScript SDK

This package is a thin authenticated client for Kiln Core's REST API. It has no
provider or workflow logic. Keep its token in server-side code or another
trusted process. Do not ship it to a browser bundle.
